name: sync_demo_server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.6 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  build_runner: ^2.3.3
  json_serializable: ^6.6.1
  lints: ^2.0.0
  test: ^1.21.0
dependencies:
  fixnum: ^1.1.0
  grpc: ^3.1.0
  logger: ^1.3.0
  orm: ^3.3.0
  synchronized: ^3.1.0
